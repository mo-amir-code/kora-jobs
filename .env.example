# Supabase project URL (base URL only, no /rest/v1/ suffix)
SUPABASE_URL=

# Supabase secret key (server-side only, bypasses RLS) — from Settings > API Keys > Secret keys
SUPABASE_SECRET_KEY=

# Name of the job to run (must match a folder name under jobs/)
JOB_NAME=
